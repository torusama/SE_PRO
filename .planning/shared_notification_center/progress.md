# Progress

- Started shared notification center implementation.
- Next: map the existing notification page, services, API contracts, and routes before editing.
- Initial broad search found the frontend notification page and backend notification module, but returned exit code 1 because the command also included a non-existent root `src` path. Results are usable; subsequent searches will target `frontend/src` and `backend/src` only.
- Mapped the frontend notification page and backend module. Existing contracts cover list/read/read-all; mark-unread and clear-all are the only missing operations needed by the requested popup.
- Next: add the two scoped backend operations and build the shared AccountActions/NotificationMenu frontend.
- Added authenticated mark-unread and clear-all backend operations scoped by `user_id`, plus service coverage.
- Added the shared AccountActions cluster, theme-aware NotificationMenu, unread badge/refresh, scrollable newest-first list, per-item read toggle, mark-all, guarded clear-all, and detail navigation. Replaced initials with the Lucide user glyph and integrated the cluster in all three header entry points.
- A test-pattern search failed because the PowerShell command used an unescaped quote sequence. No files were changed; retry with single-quoted search syntax.
- Retried the test-pattern search successfully (no existing notification API mocks) and confirmed AccountActions integration coverage. Next: remove the dead Home bell CSS and add popup interaction tests.
- Removed the obsolete Home bell CSS and added focused popup tests for unread badge/all-type rendering, read/unread toggles, mark-all, and confirmed clear-all.
- Next: run backend/frontend tests, scoped lint, and production build; fix any verification issues.
- First frontend verification found two issues: AdminHeader's old one-button assertion must now expect the requested bell plus account trigger, and the popup's open-time refresh could race an immediate optimistic read toggle in tests (and fast user interaction). Remove the redundant refresh when data is already loaded and update the header expectation.
- Frontend/backend focused tests now pass. First lint pass flagged the initial notification load as a synchronous state-setting call inside an effect; defer the initial load to the event loop while retaining cleanup, polling, and focus refresh.
- Frontend scoped lint passes after deferring initial load. Backend lint is blocked primarily by the repository's CRLF Prettier policy on edited files, plus two pre-existing-style strict issues in the service test; run the project formatter, then address only remaining semantic lint findings.
- Formatted the three edited backend notification files. Remaining lint errors are isolated to the transaction mock's `any`/unnecessary async typing; patch it with a typed Promise callback.
- Replaced the transaction test mock with a typed callback and typed authenticated notification controller users; backend lint is fully clean.
- Final verification passes: frontend 7/7 focused tests, backend 5/5 notification tests, scoped lint on both projects, frontend production build, backend Nest build, and diff hygiene.
- All plan phases are complete.
- Runtime check shows Vite live on 5173 but no backend listener on common project ports. Script inspection confirmed `npm run start:dev`; the combined port search partially failed because PowerShell passed an invalid wildcard path to ripgrep. Retry with concrete config/source paths, then start the backend hidden.
- Backend dev process was launched hidden, but port 3001 did not open within five seconds and the listener check returned no result. Run a bounded foreground startup to capture the actual initialization error instead of repeatedly spawning hidden processes.
- Foreground diagnostic revealed the project `.env` uses port 5000; it failed with `EADDRINUSE` because the earlier hidden dev launch had actually succeeded there. Confirmed port 5000 is listening and `/api/notifications` returns the expected unauthenticated 401. Frontend remains live on 5173.
- Simplified the popup after visual feedback: whole-row click now marks unread items and opens immediately; removed the per-row toggle and unread accent bar; moved mark-all/clear-all into icon-only 32px footer controls; reduced header/footer padding; made the detail button compact; and set the scroll area to a two-row minimum with a tighter 310px cap.
- Focused popup/Admin tests pass (5/5), scoped lint passes, and the frontend production build succeeds after the compact redesign.
- Reworked the popup again from established notification-center patterns after visual feedback: clicking a row now only marks it read and keeps the panel open; only "Xem tất cả thông báo" navigates. Removed the artificial list minimum height, moved icon actions into the compact header, added a subtle unread dot, tightened row hierarchy, and reduced the panel width. Focused tests pass (5/5), scoped lint passes, frontend build passes, and `git diff --check` is clean.
- Started real-data/live-refresh follow-up: remove the six temporary dev preview items, audit API wiring end to end, and replace the 60-second refresh with a safe one-second live loop.
- Initial producer search found direct notification SQL in reservations and cemetery-services plus service-based creation in contracts and reminders. One broad search returned exit code 1 because `backend/prisma` does not exist; subsequent inspection targets `backend/src` only.
- Removed all six temporary preview items. Added one-second real-API polling to the shared popup and full customer notification page, guarded against overlapping requests and stale responses during mutations. Added `no-store` to notification list/count responses and a focused no-overlap polling test.
- A credential search returned exit code 1 after finding the documented seed credentials because one requested glob/path produced no match; the useful seed result is valid. The first authenticated runtime API script then failed with a PowerShell null-reference while assuming the login response shape; retry by inspecting response keys without printing token values.
- A second PowerShell `Invoke-WebRequest` diagnostic repeated the host null-reference before response parsing; switched to Node's native `fetch`, confirmed the login response shape without printing credentials/tokens, then verified the protected notification endpoint successfully.
- Final verification passes: frontend notification/Admin tests 6/6, backend notification tests 5/5, scoped lint on both projects, both production builds, no preview-code hits, authenticated live API HTTP 200 with canonical fields and no-store caching, and clean `git diff --check`.
