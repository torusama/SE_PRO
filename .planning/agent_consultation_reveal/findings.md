# Findings

- The screenshot shows the contextual map and selected-plot card appearing without enough narrative lead-in.
- The map geometry appears visually skewed/tilted, likely from an SVG/CSS transform or source viewBox geometry rather than the recommendation data.
- Desired behavior is progressive: consultative text first, then map, then individual plot information.
- The shared prompt source is valid UTF-8; the apparent mojibake came from the shell's default decoding. The real prompt issue was insufficiently specific consultation structure, not corrupted bytes.
- The final-response prompt only says “helpful” plus basic grounding rules; it does not define a consultation structure, minimum useful content, discovery behavior, transitions, trade-offs, or the next-question pattern.
- `AgentPage` currently appends the completed assistant message and opens the contextual map in the same synchronous response handler, so map and details appear immediately with no narrative staging.
- `AgentContextMap` immediately selects the first focused plot and renders its detail card as soon as map data resolves.
- `GuidedTourMap` applies an SVG rotation based on `camera.rotation`; the contextual map inherits this behavior even though the requested consultation view should stay north-up/straight.
- `getCameraForPlots` explicitly assigns `-1.5°` for a single plot, and the map toolbar lets users rotate ±6°. The whole SVG content is wrapped in a rotating `<g>`.
- Recommendation cards inside the chat also render immediately under the assistant text, so delaying only the right map would still create the same abrupt “data dump” feeling.
- `ChatMessage` has no presentation state. Fresh responses and restored history currently render identically; animation should be opt-in only for a newly received assistant message.
- The deterministic `describeRecommendations` fallback is only one generic sentence plus the first option code/price. It must be strengthened too, otherwise provider narration failures will still feel sparse.
- Recommendation data already contains enough grounded material for richer narration: zone, codes, plot candidates, directions, area, total cost, reasons, trade-offs, and alternatives.
- Prompt v4 now specifies discovery behavior, exploratory defaults, a 120–220 word recommendation structure, grounded reasons/trade-offs, alternatives, and one concrete next question.
- The deterministic fallback now uses the same grounded consultation shape instead of a one-line result announcement.
- Fresh responses opt into paced Markdown; their cards are held until narration completes, then the right map opens after a short transition.
- The contextual plot card delays and staggers its fields, while reduced-motion users receive an immediate presentation.
- SVG rotation and rotate controls were removed; camera targets are always `rotation: 0`.
- A live exploratory chat returned HTTP 201 in 11.2 seconds with three real recommendations and a 132-word grounded consultation containing criteria, a preferred option, reasons, a verification point, alternatives, and a concrete next question.
- Raw backend zone names still contain legacy mojibake in database text, but the existing frontend display sanitizer repairs these labels before rendering.
- The latest request intentionally changes the earlier exploratory default: plain “giới thiệu đi” must enter discovery, while explicit “chọn đại/không cần hỏi” may browse immediately.
- Planner validation currently enforces budget/count only for `rank_plot_options`; `browse_available_plots` can bypass discovery and defaults to one plot before validation.
- The assistant Markdown sanitizer only removes leaked tool payloads. It does not call the display text normalizer, which is why cards are clean while the narrative still shows `Khu D â€” BÃ¬nh DÃ¢n`.
- The current display normalizer repairs punctuation and a few direction words but not common corrupted Vietnamese zone words such as `BÃ¬nh` and `DÃ¢n`.
- PostgreSQL confirmed all four active zone names were corrupted at rest, not merely during rendering.
- Migration 013 corrected the authoritative A–D zone names/descriptions; a post-migration query returns clean Vietnamese for every active zone.
- Discovery is now enforced outside the model as well: recommendation actions without budget/count receive a focused question unless the customer explicitly delegates the choice.
- Markdown prose now passes through an encoding repair that preserves line breaks and Markdown, so persisted historical messages are repaired without changing their database records.
- Exact live phrase `giới thiệu đi bé` now returns intent `clarification`, zero recommendations, no broken-font markers, and asks for both plot count and budget.
- Explicitly delegated browsing remains available, but unsupported Bazi/fortune and “ready for deposit” claims are rejected by the grounded narration validator.
- The composer regression comes from competing viewport calculations: `CustomerLayout` is only `min-height: 100vh`, while `AgentPage` independently uses `height: calc(100dvh - 77px)` plus large welcome/composer minimums. This can extend under an overlaying desktop taskbar.
- A single `100dvh` flex boundary on the customer layout, a non-shrinking navbar, and `min-height: 0` on the nested agent flex containers keep the composer and disclaimer inside the visible viewport.
- The contextual map close action is now a panel-relative, high-z-index 40px floating control, so it remains visible independently of the map header content.
- A 1920x900 live Chrome smoke screenshot confirms the composer is fully visible and centered; frontend tests pass 33/33, focused lint passes, and the production build succeeds.
- The robust layout boundary is the customer shell: make the agent layout exactly `100dvh`, let the navbar stay non-shrinking, give the main region `min-height: 0`, and let `AgentPage` consume `height: 100%`.
- A close button already exists inside the contextual-map header, but it is not independently positioned. It can disappear with a cropped/scrolled panel, so the control should be elevated to an absolute, high-z-index panel control.
