// Compatibility re-export. The active prompt lives in ./prompts so there is
// only one semantic-routing policy to maintain.
export { CEMETERY_AGENT_SEMANTIC_ROUTER_PROMPT } from './prompts/cemetery-agent.semantic-router-prompt';
